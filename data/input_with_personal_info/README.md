# `input_with_personal_info`

This folder contains unprocessed dowloads from MyTurn. These downloads contain personal information like member names and addresses, and is ignored in .gitignore to prevent this data from being added to source control. See the top level project README for instructions on how to anonymize this data so it can be used as input.